# 視覺系統

依實作記錄，不是預先寫的規則書。
2026-08-06 起以 `app.css` 為準，`prototype.html` 是當時的定格。

字級全部用 rem，根字級 16px，設定頁的「字級：大」把它換成 17.5px，
所以整套一起縮放。間距維持 px，不跟著長。

**配色保留成使用者可切換，不收斂成單一方向。** 使用者 2026-08-06 決定
要能自己切換，所以剩下的方向都留在正式版，切換入口放設定頁，選擇存
localStorage。這改變了原本「選定一個方向後本文件收斂」的計畫，
連帶所有元件都不可以寫死顏色，一律走語意變數。

**計器方向已於 2026-08-06 依使用者指示刪除。** 只剩版下與常夜灯，
預設是常夜灯。

## 世界

**版下（印刷指定紙）× 夜間計器盤。**

一課是一張印刷規格單，語感是一支有刻度的量尺。左側留白欄放邊註，
頂部是逐卡點亮的刻度レール，言い換えカード用五段檢刻的量尺標示
每個說法落在哪一級。

拒絕的兩個既定長相：Duolingo 式圓角遊戲化卡片與吉祥物，
Anki 式灰白極簡表單。兩者都是這個類別的窠臼。

## 排版

字體全部用系統內建，不外載 webfont。理由是離線可用與開啟速度，
而且 iOS 內建的 Hiragino 品質足夠。這是刻意決定，不是退而求其次。

| 用途 | 字體堆疊 | 變數 |
|---|---|---|
| 日文內容（例句、文型名、單字） | Hiragino Mincho ProN, Yu Mincho, Noto Serif JP | `--f-mi` |
| 介面與標籤、くだけた的例句 | Hiragino Sans, Hiragino Kaku Gothic ProN, Noto Sans JP | `--f-go` |
| 數字、課號、刻度 | ui-monospace, SF Mono, Menlo | `--f-num` |

**明朝與ゴシック的分工本身承載意義。** 內容用明朝，介面用ゴシック。
在言い換えカード裡，くだけた與丁寧的例句用ゴシック，改まった與格式
用明朝。日文印刷傳統裡這組對立本來就對應場面的正式度，讓字體
跟著語感軸線走，是這個產品才做得到的事。

日文不用斜體。`<em>` 一律改成顏色與字重強調，因為日文沒有斜體傳統，
系統字的斜體是合成拉斜的，很難看。

尺寸於 2026-08-06 依使用者要求整體放大一級半（×1.15），
睡前眼睛累，原本的級數偏小。`.ja` 的行距同時由 1.95 收到 1.85，
因為字級變大後同樣的比例會撐出比原設計更多的絕對留白。

| 級別 | 尺寸 | 用途 |
|---|---|---|
| `.h1` | 31px | 單元名 |
| `.h2` | 25.5px | 文型名 |
| `.ja` | 22px / 1.85 | 例句 |
| `.wz` | 19.5px | 單字中譯 |
| `.zh` | 15.5px | 例句中譯 |
| `.rd` | 14.5px | 読み（可關閉） |
| `.lbl` | 12.5px / .14em | 等寬小標 |

放大的代價已量測：393×745（iPhone 15 Safari 含工具列）下需要捲動的
畫面由 35 分之 6 增為 35 分之 13，最長的一張由 425px 增為 624px。
集中在文型卡與言い換えカード第二三段，這兩種卡本來就是最密的。
若之後覺得捲太多，要動的是內容密度（例句數、variant 數），不是字級。

## 配色

三個方向共用同一組語意變數，只換值。深灰底不用純黑，淺灰字不用純白。

| 變數 | 版下 hanshita | 常夜灯 joyato |
|---|---|---|
| `--bg` | `#14161F` | `#191512` |
| `--bg2` | `#1C1F2B` | `#221D18` |
| `--tx` | `#E4E0D8` | `#E8DDCD` |
| `--tx2` | `#948FA3` | `#A3947F` |
| `--tx3` | `#615D70` | `#6E6355` |
| `--sig` | `#E2564C` 朱 | `#7FA88B` 青磁 |
| `--rule` | `#2A2E3C` | `#2E271F` |

版下是墨藍配校對朱，常夜灯是暖褐配青磁，較柔和，為預設。
兩者在正式版都是使用者可切換的選項，不是設計者二選一的候選。

重點色只用在：邊註、刻度亮點、語感級別標籤、槓桿關鍵詞、主按鈕。
不用漸層文字，不用光暈。

## 版面

- `.wrap` 最大 560px 置中。`?w=390` 可框到 iPhone 寬度預覽。
- `.margin` 38px 留白欄，右側 1px 分隔線貫穿全高。欄內放課號
  （等寬三位數）、文型編號、卡片類型記號。
- 內容卡一律**上緣對齊**，只有主題卡與完了卡垂直置中。
  理由是每晚要連續點過十七張卡，固定的起始視線位置比構圖平衡重要。
- 圓角 10 到 14px。層次只用底色差，不疊陰影與邊框。

## 翻頁

畫面左右各半是兩個透明點擊區，**右半前進，左半後退**，
使用者 2026-08-06 指定。左右滑動同時保留。

底部那條同時是兩個區的標示：左下 `‹ もどる`、中間 `01 / 17` 進度、
右下 `すすむ ›`。標示本身落在各自的點擊區裡，點文字就等於點那一半，
不是另外做的按鈕。第一張卡時左側標示降到 22% 不透明度。
言い換えカード的三段展開走同一組手勢，右半推進段落，左半退回。

複習卡的選項按鈕與完了卡的兩顆按鈕疊在點擊區之上，
點選項不會誤觸翻頁。

**第一張卡按左半回首頁。** standalone 沒有瀏覽器返回鍵，
所以底部左側的標示在第一張卡會從「もどる」換成「ホーム」。
設定頁另外有左上角的返回鍵，尺寸 44×44。

所有可點的東西不小於 44×44，包含首頁的次要連結與設定頁的分段控制。
這條是躺著單手操作的硬要求，量過才算數。

## 動態

只有一個：卡片切換時內容 `translateY(7px)` 加淡入，260ms，
`cubic-bezier(.16,1,.3,1)`。刻度與量尺的變化用 `transform: scaleY()`
不用 height，避免 layout thrash。`prefers-reduced-motion` 時全部關閉。

## 圖示

全部自繪 SVG，1px 描邊，不用 emoji 也不用 unicode 字元。
目前三個：返回箭頭、量尺記號（言い換えカード的邊註）、
復習回指記號。量尺記號是產品自己的符號，一條底線加三根不等高的刻度。

## 瀏覽器表面

選取色用 `--sig-dim`，focus ring 用 `--sig`，捲軸用 `--rule2`。
`-webkit-tap-highlight-color` 關閉，按下狀態自己做。

## iOS

`viewport-fit=cover` 加 `env(safe-area-inset-*)`，高度用 `100dvh`，
`touch-action:manipulation`，`overscroll-behavior:none`，
`-webkit-text-size-adjust:100%`。細節見規劃文件的 iOS 專節。
